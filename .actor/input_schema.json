{
  "schemaVersion": 1,
  "title": "Cutshort Jobs Scraper Input",
  "type": "object",
  "properties": {
    "skillKeyword": {
      "title": "Skill Keyword",
      "type": "string",
      "description": "Job skill/role to search for (e.g., 'python', 'react', 'data-science', 'product-manager'). Use hyphenated format for multi-word skills.",
      "editor": "textfield",
      "prefill": "python"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "City name (e.g., 'bangalore', 'delhi', 'mumbai', 'pune', 'hyderabad'). Leave empty for all India.",
      "editor": "textfield",
      "prefill": "bangalore"
    },
    "maxResults": {
      "title": "Max Results",
      "type": "integer",
      "description": "Maximum number of job listings to scrape",
      "prefill": 50,
      "minimum": 1
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings (RESIDENTIAL recommended)",
      "editor": "proxy",
      "default": {"useApifyProxy": true, "apifyProxyGroups": ["RESIDENTIAL"]}
    }
  },
  "required": ["skillKeyword", "maxResults"]
}
